# Copia este fichero a `.env` y rellena las claves reales.
# El archivo `.env` está en .gitignore para evitar subir secretos al repositorio.
# NUNCA pongas claves reales en este archivo (.env.example SÍ se commitea).

# Clave API de Google AI Studio (Gemini)
# Obtener en: https://aistudio.google.com/app/apikey
# Es gratuita; el plan free tiene cupo diario suficiente para desarrollo.
GEMINI_API_KEY=****
